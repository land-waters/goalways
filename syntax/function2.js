console.log(Math.round(1.6));
console.log(Math.round(1.4));
// round 함수는 입력값을 반올림

function sum(first,second){
  return first + second;
}

console.log(sum(2,4));

/*function에서 return을 할 경우 그 값을 읽기 위해 console.log 사용
이와 같이 Math.round 함수도 console.log를 쓰지 않으면 값 출력 x */
